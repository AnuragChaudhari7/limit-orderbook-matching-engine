#include "OrderBook.h"

OrderBook::OrderBook(){
    /*Default map & deque constructors*/
}

unsigned int OrderBook::submitLimitOrder(unsigned int quantity, bool type, double price){
    //TODO: Implement. Need to figure out how to construct the Limit Order (unique id & timestamp and where those go?)
    // Then you need to call the matching function or implement the logic here itself. Figure it out.
    return 0;
}